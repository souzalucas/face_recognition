syntax = "proto3";

service recognitionFacial {
  rpc uploadRecognition(stream Chunk) returns (Reply) {}
  rpc recognition(stream Chunk) returns (Reply) {}
}

message Chunk {
  bytes buffer = 1;
  string nameFile = 2;
  string identifier = 3;
  string ipPort = 4;
  int32 qtde = 5;
}

message Request {
  string name = 1;
}

message Reply {
  string message = 1;
  int32 length = 3;
}
